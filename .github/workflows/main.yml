name: CI

on: 
  push:
    branches:
      - master
        
jobs:
  build:
    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@master
      
    - name: Build the site
      run: echo "Building a site"

    - name: Deploy
      uses: ./
      
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.KEY }}
        port: ${{ secrets.PORT }}
        source: "./docs"
        target: ${{ secrets.LOCATION }}
